// src/kit/useReducedMotion.ts
// Thin re-export of framer-motion's useReducedMotion so kit consumers
// don't need to import from framer-motion directly.
export { useReducedMotion } from 'framer-motion'
