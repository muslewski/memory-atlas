# memory-atlas

A convention and toolkit for per-repository knowledge bases — code-verified
architecture maps, decision ledgers, and agent-navigable memory.

Status: pre-release — see SPEC.md

**This repo eats its own cooking**: see `atlas/` for this project's own
Atlas (zone cards over `bin/`, `lib/`, `docs/`, `schema/`, `skills/`, and
`adapters/`), start at `atlas/map/index.md`. Migrating an existing repo?
See `docs/ADOPTION.md`.
